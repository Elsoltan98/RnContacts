import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  floatButton: {
    backgroundColor: 'red',
    width: 50,
    height: 50,
    borderRadius: 100,
    alignItems: 'center',
    justifyContent: 'center',
    position: 'absolute',
    top: 20,
    right: 15,
  },
});
